[project]
name = "ai-research-assistant"
version = "0.1.0"
description = "RAG assistant with FAISS + Grok"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]